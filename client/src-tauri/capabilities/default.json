{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the MaaToolbox client.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
